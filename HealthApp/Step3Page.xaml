<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HealthApp.Step3Page">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
            BackgroundColor="#F5CDAA">

            <Label
                Text="Step 3"
                HorizontalOptions="Fill"
                TextColor="#825024"
                BackgroundColor="#EDB27E"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Please type in the third step in your plan"
                FontSize="Small"
                Padding="25"
                HorizontalOptions="Fill"
                TextColor="#825024"
                BackgroundColor="#EDB27E"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Entry
                x:Name="Entry"
                Placeholder="Enter text"
                TextColor="#000000"
                BackgroundColor="#D9D9D9"
                VerticalOptions="Fill"
                 />
            
            <Button
                x:Name="Step3Btn"
                Padding="40"
                Text="Step 4" 
                TextColor="#255B65"
                BackgroundColor="#7EDCED"
                FontSize="Medium"
                VerticalOptions="Fill"
                Clicked="OnStep4Click"
                 />

            <Button
                x:Name="BackBtn"
                Padding="30"
                Clicked="OnBackClick"
                HorizontalOptions="Start"
                TextColor="#285F69"
                BackgroundColor="#7EDCED"
                Text="Back" 
                FontSize="Medium"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
